services:
  - type: web
    name: iptv-zeus
    env: node
    build:
      dockerfile: Dockerfile
    envVars:
      - key: REACT_APP_DNS
        value: # Removido valor placeholder
      - key: REACT_APP_API_URL
        value: # Removido valor placeholder